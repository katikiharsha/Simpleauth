<%= form_for(@usersession) do |f| %>
  <% if @usersession.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usersession.errors.count, "error") %> prohibited this usersession from being saved:</h2>

      <ul>
      <% @usersession.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sign_in %><br>
    <%= f.text_field :sign_in %>
  </div>
  <div class="field">
    <%= f.label :create %><br>
    <%= f.text_field :create %>
  </div>
  <div class="field">
    <%= f.label :sign_up %><br>
    <%= f.text_field :sign_up %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
